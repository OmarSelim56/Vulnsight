# VulnSight Sensor Agent — minimal dependencies
# Install with: pip install -r sensor/requirements.txt

# Network flow capture
nfstream>=6.5.3

# ML inference (must match version used when the model was trained)
torch>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
